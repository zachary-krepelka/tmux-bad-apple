source docs/exhibits/shared.sh

pace $(awk '/SPEED_0/{print $3}' makefile)

curtain_up

cmd 'clear; bash scripts/view.sh -m media/exhibit0.tvid'

sleep_until_percent -e 0.5 $((shared_target_percent * 2))

curtain_down

# vim: ft=bash
